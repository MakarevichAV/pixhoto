import React from 'react';
import s from './Exemple.module.css';

const Exemple = () => {
    return (
        <div className={s.centrBlock}>
            <p className={s.txt}>Exemple Block</p>
        </div>
    );
}

export default Exemple;